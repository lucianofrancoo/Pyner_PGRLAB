# 🚀 PYNER: Query Generation MVP (Sesgado - 500K) - Copia en Query_generator/

Este fichero es una copia del guía rápida para probar el generador
de queries desde la nueva carpeta `Query_generator/`.

Comandos rápidos:

```bash
# Test rápido completo
python3 Query_generator/quick_test.py

# Test interactivo de queries
python3 Query_generator/test_queries.py --interactive

# Test específico
python3 Query_generator/test_queries.py "virus humanos"
```

Nota: Datos sesgados (500K). Phase 3b añadirá NCBI integration.
